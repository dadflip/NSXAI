# =============================================================================
# NSXAI — Dépendances globales
# Installation : pip install -r requirements.txt
# =============================================================================

# --- 1. CORE : Ontologie & Raisonnement ---
owlready2==0.50          # Manipulation d'ontologies OWL
rdflib==7.0.0            # Lecture/écriture de données RDF

# --- 2. KNOWLEDGE GRAPH : Traitement des graphes et données ---
networkx==3.2            # Structures de graphes
pandas                   # Manipulation de données tabulaires
numpy                    # Calcul numérique
scikit-learn             # Algorithmes de ML (PCA, centralités, etc.)
pyarrow                  # Support pour Parquet/Feather (lecture/écriture rapide)
fastparquet              # Alternative à pyarrow pour Parquet

# --- 3. API & HTTP ---
fastapi==0.115.0         # Framework API Python
uvicorn[standard]==0.32.0 # Serveur ASGI
python-multipart==0.0.20 # Support des formulaires / uploads
requests==2.32.3         # Requêtes HTTP
SPARQLWrapper==2.0.0     # Requêtes SPARQL vers Fuseki

# --- 4. UTILITAIRES ---
python-dotenv==1.0.0     # Gestion des variables d'environnement (.env)
PyYAML==6.0.2            # Parsing YAML (pour les configs)
psutil                   # Gestion avancée des processus OS (utilisé par le CLI)
matplotlib               # Visualisation (utilisé par le pipeline)
seaborn                  # Visualisation (utilisé par le pipeline)
papermill

# =============================================================================
# 5. MACHINE LEARNING AVANCÉ (GNN, XGBoost)
# =============================================================================
torch==2.12.0
xgboost
# torch-geometric==2.4.0